import { fixture, expect } from '@open-wc/testing';
import '../../src/routes/page-login';

it('can instantiate an element', async () => {
  const el = await fixture('<page-login></page-login>');
  // expect
});
